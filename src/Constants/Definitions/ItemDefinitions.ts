import ItemType from 'Constants/ItemType';

const ItemDefinitions = {
  MIRROR: new ItemType('mirror'),
  SHIELD: new ItemType('shield'),
  SHOVEL: new ItemType('shovel')
};
export default ItemDefinitions;
